[pytest]
DJANGO_SETTINGS_MODULE = src.document_search.test_settings
pythonpath = src
#django_find_project = false
addopts =
    --verbose
    --tb=short
    --strict-markers
    --disable-warnings
    --reuse-db
    --no-cov-on-fail
    --cov-branch
    --ds=document_search.test_settings
env =
    DJANGO_SETTINGS_MODULE = document_search.test_settings
testpaths = tests
python_files = tests.py test_*.py *_tests.py
python_classes = Test* *Test *Tests
python_functions = test_*

# Метки для фильтрации тестов
markers =
    slow: marks tests as slow (deselect with '-m "not slow"')
    integration: marks tests as integration tests
    unit: marks tests as unit tests
    api: marks tests as API tests
    models: marks tests as model tests
    algorithms: marks tests as algorithm tests

# Настройки фильтрации предупреждений
filterwarnings =
    ignore::DeprecationWarning
    ignore::PendingDeprecationWarning
    ignore::django.utils.deprecation.RemovedInDjango50Warning
    ignore::pytest.PytestUnraisableExceptionWarning

# Минимальная версия pytest
minversion = 6.0