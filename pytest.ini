[tool:pytest]
DJANGO_SETTINGS_MODULE = document_search.settings
python_paths = src
addopts =
    --verbose
    --tb=short
    --strict-markers
    --disable-warnings
testpaths = tests
python_files = test_*.py
python_classes = Test* *Test *Tests
python_functions = test_*